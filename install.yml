    - hosts: all
      tasks:
        - name: install apache2 pour debian
          package:
            name: "webserver" 
            
        - name: Installer quelques dépendances 
          package:
            name: 
              - git
